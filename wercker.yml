box: shaperid/cypress-python

setup:
  steps:
    - script:
	      name: A
        code: |
          echo Testing the WERCKER_CACHE_DIR environment variable
          echo $WERCKER_CACHE_DIR

test:
  steps:
    - script:
	      name: B
        code: |
          echo Testing the WERCKER_CACHE_DIR environment variable
          echo $WERCKER_CACHE_DIR

