box: shaperid/cypress-python

setup:
  steps:
    - script:
      name: Test env variable in setup pipeline
      code: |
        echo "Testing WERCKER_CACHE_DIR location"
        echo $WERCKER_CACHE_DIR

test:
  steps:
    -script:
      name: Test env variable in test pipeline
      code: |
        echo "Testing WERCKER_CACHE_DIR location"
        echo $WERCKER_CACHE_DIR
